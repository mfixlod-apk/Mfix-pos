# MFIX Android
Android wrapper for the MFIX POS/repairs HTML app with native USB ESC/POS printing.

GitHub Actions builds `app-debug.apk` automatically. Download it from the `MFIX-APK` artifact of the latest successful workflow run.
